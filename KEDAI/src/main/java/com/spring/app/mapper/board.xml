<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">

	<select id="category_select" resultType="com.spring.app.domain.CategoryVO">
		select category_code, category_name
		from tbl_category
		order by category_code asc
	</select>
	
	<select id="getGroupnoMax" resultType="int">
		select nvl(max(groupno), 0)
		from tbl_board
	</select>
	
	<insert id="add" parameterType="BoardVO">
		<!-- 원글쓰기인 경우 -->
		<if test='fk_seq.equals("")'>
			insert into tbl_board(board_seq, to_number(fk_category_code), fk_empid, name, subject, content, pwd, read_count, registerday, status, groupno, fk_seq, depthno)
			values(board_seq.nextval, #{fk_category_code}, #{fk_empid}, #{name}, #{subject}, #{content}, #{pwd}, default, default, default, #{groupno}, default, default)
		</if>
		
		<!-- 답변글쓰기인 경우 -->
		<if test='!fk_seq.equals("")'>
			insert into tbl_board(board_seq, to_number(fk_category_code), fk_empid, name, subject, content, pwd, read_count, registerday, status, groupno, fk_seq, depthno)
			values(board_seq.nextval, #{fk_category_code}, #{fk_empid}, #{name}, #{subject}, #{content}, #{pwd}, default, default, default, #{groupno}, #{fk_seq}, #{depthno}+1)
		</if>
	</insert>
	
	<insert id="add_withFile" parameterType="BoardVO">
		<!-- 원글쓰기인 경우 -->
		<if test='fk_seq.equals("")'>
			insert into tbl_board(board_seq, to_number(fk_category_code), fk_empid, name, subject, content, pwd, read_count, registerday, status, groupno, fk_seq, depthno, orgfilename, filename, filesize)
			values(board_seq.nextval, #{fk_category_code}, #{fk_empid}, #{name}, #{subject}, #{content}, #{pwd}, default, default, default, #{groupno}, default, default, #{orgfilename}, #{filename}, #{filesize})
		</if>
		
		<!-- 답변글쓰기인 경우 -->
		<if test='!fk_seq.equals("")'>
			insert into tbl_board(board_seq, to_number(fk_category_code), fk_empid, name, subject, content, pwd, read_count, registerday, status, groupno, fk_seq, depthno, orgfilename, filename, filesize)
			values(board_seq.nextval, #{fk_category_code}, #{fk_empid}, #{name}, #{subject}, #{content}, #{pwd}, default, default, default, #{groupno}, #{fk_seq}, #{depthno}+1, #{orgfilename}, #{filename}, #{filesize})
		</if>
	</insert>
	
	
	
	
	
	
	
	
	
	
	
	
	
	<update id="pointPlus" parameterType="HashMap">
		update tbl_employees set point = point + to_number(#{point})
		where empid = #{empid}
	</update>
	
</mapper>